info:
  name: Get Sound Theme
  type: http
  seq: 4

http:
  method: get
  url: "{{BaseURL}}/sounds/theme/get"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
