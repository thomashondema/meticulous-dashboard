info:
  name: List Sound Themes
  type: http
  seq: 3

http:
  method: get
  url: "{{BaseURL}}/sounds/theme/list"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
