info:
  name: Get Root Password
  type: http
  seq: 4

http:
  method: get
  url: "{{BaseURL}}/machine/root-password"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
