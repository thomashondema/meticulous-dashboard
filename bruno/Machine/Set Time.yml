info:
  name: Set Time
  type: http
  seq: 5

http:
  method: post
  url: "{{BaseURL}}/machine/time"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
