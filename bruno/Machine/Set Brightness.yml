info:
  name: Set Brightness
  type: http
  seq: 2

http:
  method: post
  url: "{{BaseURL}}/machine/backlight"
  headers:
    - name: Accept
      value: application/json
  body: {}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
