info:
  name: Connect to WiFi
  type: http
  seq: 5

http:
  method: post
  url: "{{BaseURL}}/wifi/connect"
  headers:
    - name: Accept
      value: application/json
  body: {}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
