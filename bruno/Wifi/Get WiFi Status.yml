info:
  name: Get WiFi Status
  type: http
  seq: 1

http:
  method: get
  url: "{{BaseURL}}/wifi/config"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
