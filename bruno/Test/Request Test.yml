info:
  name: Request Test
  type: http
  seq: 1

http:
  method: get
  url: "{{BaseURL}}/test/:test"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: test
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
