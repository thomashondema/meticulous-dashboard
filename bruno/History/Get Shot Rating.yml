info:
  name: Get Shot Rating
  type: http
  seq: 8

http:
  method: get
  url: "{{BaseURL}}/history/rating/:shotId"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: shotId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
