info:
  name: List Profiles (Short)
  type: http
  seq: 3

http:
  method: get
  url: "{{BaseURL}}/profile/list"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
