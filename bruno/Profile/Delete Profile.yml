info:
  name: Delete Profile
  type: http
  seq: 8

http:
  method: delete
  url: "{{BaseURL}}/profile/delete/:profileId"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: profileId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
