info:
  name: Get Profile
  type: http
  seq: 7

http:
  method: get
  url: "{{BaseURL}}/profile/get/:profileId"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: profileId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
