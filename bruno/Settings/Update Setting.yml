info:
  name: Update Setting
  type: http
  seq: 3

http:
  method: post
  url: "{{BaseURL}}/settings/"
  headers:
    - name: Accept
      value: application/json
  body: {}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
